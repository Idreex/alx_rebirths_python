#!/usr/bin/python3

def multiple_returns(sentence):
    
    wc = len(sentence)
    ini = sentence[0]
    print(f"Length of sentence is {wc} and first letter is {ini}")

multiple_returns("Alasoro No1") 
    